name: gateway_adapter
description: Compatibility adapter around the current OpenClaw Gateway surface.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.4.0

resolution: workspace

dependencies:
  gateway_transport:
    path: ../gateway_transport

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.2
