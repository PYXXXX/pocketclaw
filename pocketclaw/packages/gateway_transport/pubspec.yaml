name: gateway_transport
description: Gateway transport primitives for PocketClaw.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

resolution: workspace

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.2
